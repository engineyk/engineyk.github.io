---
layout:     post
title:      观察者模式
subtitle:   设计模式
date:       2024-04-09
author:     engineyk
header-img: img/post-bg-ocenwar.jpg
catalog: true
tags:
    - 设计模式
---

# 观察者模式（Observer Pattern）—— C# 实现

观察者模式是一种行为型设计模式，定义对象之间的一种一对多依赖关系，使得当一个对象的状态发生改变时，所有依赖于它的对象都会得到通知并自动更新。

## 结构

- **Subject（主题/被观察者）**：维护观察者列表，提供添加、删除观察者的方法，并在自身状态变化时通知所有观察者。
- **Observer（观察者）**：定义在接收到通知时应执行的操作。
- **ConcreteSubject（具体主题）**：实现主题接口，维护具体状态。
- **ConcreteObserver（具体观察者）**：实现观察者接口，接收并响应通知。

## UML 类图

```
[ConcreteObserverA]   [ConcreteObserverB]
         \                  /
          \                /
            [Subject/ConcreteSubject]
```

## C# 示例代码

```csharp
using System;
using System.Collections.Generic;

// 观察者接口
public interface IObserver
{
    void Update(int state);
}

// 主题接口
public interface ISubject
{
    void Attach(IObserver observer);
    void Detach(IObserver observer);
    void Notify();
}

// 具体主题
public class ConcreteSubject : ISubject
{
    private List<IObserver> observers = new List<IObserver>();
    private int _state;

    public int State
    {
        get { return _state; }
        set
        {
            _state = value;
            Notify();
        }
    }

    public void Attach(IObserver observer) => observers.Add(observer);
    public void Detach(IObserver observer) => observers.Remove(observer);

    public void Notify()
    {
        foreach (var observer in observers)
        {
            observer.Update(_state);
        }
    }
}

// 具体观察者A
public class ConcreteObserverA : IObserver
{
    public void Update(int state)
    {
        Console.WriteLine("ConcreteObserverA: State changed to " + state);
    }
}

// 具体观察者B
public class ConcreteObserverB : IObserver
{
    public void Update(int state)
    {
        Console.WriteLine("ConcreteObserverB: State changed to " + state);
    }
}

// 使用示例
class Program
{
    static void Main()
    {
        ConcreteSubject subject = new ConcreteSubject();
        IObserver observerA = new ConcreteObserverA();
        IObserver observerB = new ConcreteObserverB();

        subject.Attach(observerA);
        subject.Attach(observerB);

        subject.State = 5;
        subject.State = 10;
    }
}
```

## 输出结果

```
ConcreteObserverA: State changed to 5
ConcreteObserverB: State changed to 5
ConcreteObserverA: State changed to 10
ConcreteObserverB: State changed to 10
```

## 适用场景

- 对象间存在一对多依赖关系，且状态变化时需要通知所有依赖者。
- 事件系统、消息订阅与发布、GUI系统等。

## 优缺点

**优点：**
- 观察者和主题耦合度低，可独立扩展。
- 支持广播通信。

**缺点：**
- 可能引起连锁更新，影响性能。
- 主题与观察者间的通知顺序不确定。

## 总结

观察者模式是事件通知和消息广播的经典实现，是多对象联动的优雅方案，广泛用于各种需要通知更新的场景。